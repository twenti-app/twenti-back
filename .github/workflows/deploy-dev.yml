name: Deploy-all

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy_eureka:
    runs-on: ubuntu-latest
    steps:
      - name: Build Eureka
        uses: ../../eureka/deploy-eureka.dev.yml